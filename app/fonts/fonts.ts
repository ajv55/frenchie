import { Arimo, Dancing_Script, Merriweather, Alex_Brush } from 'next/font/google';
 
export const arimo = Arimo({ subsets: ['latin'] });

export const dancingScript = Dancing_Script({ subsets: ['latin'] });

export const merriweather = Merriweather({subsets: ['latin'], weight: ['400']});

export const alex_brush = Alex_Brush({subsets: ['latin'], weight: ['400']});

